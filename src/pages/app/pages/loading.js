import React from 'react'
import Link from 'gatsby-link'
import styles from "../index.module.css"

const Loading = () => (
  <div className={styles.container}>
    <h1 className={styles.tagline}>Loading...</h1>
  </div>
)

export default Loading
